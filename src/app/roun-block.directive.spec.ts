import { RounBlockDirective } from './roun-block.directive';

describe('RounBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new RounBlockDirective();
    expect(directive).toBeTruthy();
  });
});
